name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up environment variables
        run: echo "API_KEY=b3025235c339480a744e5fe52a84dc0d" >> $GITHUB_ENV

      # Add more steps for your build, test, and deployment process
